import Card from "./ui/card";

export default function ResultsTable({ runResult }) {
  console.log('results table', runResult);

  return (
    <Card>
      <p>Will return a Table of Results</p>
    </Card>
  );
}
