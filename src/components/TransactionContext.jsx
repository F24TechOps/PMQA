import Card from "./ui/card";

export default function TransactionContext() {
  return (
    <Card>
      <div>
        This is the transcation context to house the dropdowns for account id,
        cycle id, and transaction id
      </div>
    </Card>
  );
}
